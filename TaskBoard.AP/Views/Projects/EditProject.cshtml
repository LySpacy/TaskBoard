@using TaskBoard.API.Contracts;
@using TaskBoard.API.Contracts.Project;
@model UpdateProjectRequest

<h2>Редактирование проекта</h2>

<form asp-action="EditProject" method="post">
    <div class="form-group">
        <input type="hidden" name="Id" value="@Model.Id">
        <label for="Title">Название:</label>
        <input type="text" class="form-control" id="Title" name="Title" value="@Model.Title" required>
    </div>
    <div class="form-group">
        <label for="Description">Описание:</label>
        <textarea class="form-control" id="Description" name="Description" required>@Model.Description</textarea>
    </div>
    <button type="submit" class="btn btn-primary">Сохранить</button>
    <a href="@Url.Action("GetProject", new { id = Model.Id })" class="btn btn-secondary">Вернуться</a>
</form>
